/*
package com.travelsky.airportapp.pager;

import android.app.Activity;
import android.view.View;
import android.widget.ExpandableListView;
import android.widget.LinearLayout;

import com.travelsky.airportapp.R;
import com.travelsky.airportapp.adapter.FlightListAdapter;
import com.travelsky.airportapp.base.BasePager;

*/
/**
 * 航班管控
 *//*

public class FlightControlPager extends BasePager {

    private LinearLayout lladd;

    private ExpandableListView lv;

    private FlightListAdapter adapter;

    public FlightControlPager(Activity activity) {
        super(activity);
    }

    @Override
    public void initData() {
        super.initData();

        //加载视图
        View view = View.inflate(mActivity, R.layout.security_details, null);

        lladd = (LinearLayout) view.findViewById(R.id.lladd);
        lv = (ExpandableListView) view.findViewById(R.id.lv);

        //adapter = new FlightListAdapter(mActivity);

        //填充数据
        lv.setAdapter(adapter);

        //把当前View添加到FrameLayout中
        fl_base_content.addView(view);

    }



}
*/
